[
  {
    "import random ": "modulo para elecciones pseudo-random; random.randint(range): selecciona un int in range; random.choice(arg): selecciona algo dentro arg; random.seed(arg): para agregar una semilla inicial.",
    "import string ": "para cosas con strings; string.punctuation: un str con los signos de puntuacion; string.ascii_upper(lower)case: devuelve el abecedario en mayuscula o minuscula.",
    "import math ": "tallas matematicas; math.sin(arg) : halla el seno de arg; math.sqrt(arg) : halla raiz cuadrada de arg; math.pi: devuelve pi; math.ceil(arg) and math.floor(arg): roundean al integer mas cercano, mas alto o mas pequeno respectivamente.",
    "import re ": "para trabajar con regex; re.search(regex, variable) and re.findall(argumento, variable): devuelve uno o todos los elementos donde aparezca la regex; re.compile() - para compilar una regex y pasarselas a otras funciones.",
    "import json ": "para trabajar con jsons; json.dump(arg,file,indent=): guarda arg en  file,indent= separacion(2 o 4); json.load(file): lee file previamente cargado con open() y devuelve todo lo dentro del json.",
    "import sys": "se utiliza principalmente en programaci\u00f3n orientada al sistema(m\u00e1s sobre el tema en SystemProgramming).",
    "import datetime": "para trabajar con fechas y horas; datetime.datetime.now() : retorna la fecha y hora actual; datetime.date() : acepta fechas para hacer calculos; datetime.date.today() : devuelve la fecha actual.",
    "import this": "el zen de python.",
    "import os": "Se utiliza en la programaci\u00f3n orientada al sistema(m\u00e1s sobre el tema en SystemProgramming).",
    "import multiprocessing": "es para levantar procesos independientes with Process. Mas en SystemProgramming.",
    "import time": "turbiedad, time.sleep(#) : detiene el proceso segun el numero de segundos introducidos.",
    "import __thread": "para trabajar con threads(lightweight)(ronnie).Todos comparten el mismo standard output.Mas en SystemProgramming.",
    "import threading": "se usa para manejar threads a alto nivel, que en muchos de sus casos usa internamente a _thread. La gran diferencia es que este es usado en OOP, heredando de una clase Thread donde podemos customizar su __init__ y la funcion run().",
    "import queue": "es un bolsillo dimensional que es usado por los threads para leer y guardar cosas. Metodos en SystemProgramming.",
    "import pandas": "pandas se usa pra procesar datos utilizando de fondo numpy.",
    "import numpy": "para trabajar con numeros, ta potente.",
    "import matplotlib.pyplot": "para visualizar datos.",
    "import seaborn": "para visualizar data, pone matploylib mas bonito(uwu).",
    "import PIL": "para trabajar con imagenes; PIL.Image.open(arg)- cargar el archivo para trabajar con el,obj.thumbnail(size) - para crear thumbnails con el size(tupla) especificado, obj.save(path) para salvarla; PIL.ImageTk.PhotoImage(file) - para cargar im\u00c3\u00a1genes directamente.",
    "import sklearn": "importa scikit-learn, para trabajar con machine learning. Revisar adentro todas las ramas y clases en la cheat sheat.",
    "import scipy": "libreria para tallas cienificas; scipy.stats.trim_mean(obj,percent) : para hallar el trimmed mean que es quitar un porcentaje de datos a cada lado(mas pequenos y mas grandes); scipy.stats.probplot(data,plot= ax) - para plotear la distribucion normal, en plot hay que especificar el subplot; scipy.stats.mode() - halla moda.",
    "import joblib": "para salvar y cargar archivos manteniendo su forma original de objetos de python(que supuestamente trabaja mas rapido que pickle ya que esta optimizado para arrays); joblib.load(arg) : carga arg; joblib.dump(arg,file) : salva arg en nombre de archivo(.joblib).",
    "import xgboost": "para trabajar con XGBClassifier() y XGBRegressor el cual es un ensemble model mejorado.",
    "import hyperopt": "libreria de optimizacion de hyperparameters; hyperopt.hp.(uniform,quniform): para seleccionar valores entre un min/max dividido entre enteros o floats respect; tambien podemos encontrar Trials(),fmin(),tpe.suggest.",
    "import mlt_class": "contiene dos clases principales,XGBs(arboles xgboost) y SVMs(SVMs, normal y linear) que se alimentan en la clase general MlT, al iniciar la clase hay que pasarle features and targets, y al utilizar una de las funciones se le debe pasar tipo de problema('c' por clasificacion y 'r' de regresion); escorer default es f1 y mae respectivamente; average default binario, 'w' para weighteado, para mas leer documentacion.",
    "import bs4": "libreria para parsear documentos web(html,xml,etc).Mas en web scraping module.",
    "import psycopg2": "para trabajar con postgresql desde python; psycopg2.connect(host,database,user,password) conecta con el server; conection_obj.cursor() - para crear el cursor; las query que uno haga se guardan en el cursor; cursor_obj.excecute(query) - para ejecutrar querys que despues se pueden obtener en cursor_obj.fetch_one() or fetch_all(); conection_obj.commit() para ejucutar los cambios hechos en la db, conn_obj.close() para cerrar la conexion.",
    "import requests": "para trabajar con internet.Mas en web scraping.",
    "import wget": "para hacer descargas; wget.download(url,out) - para descargar un archivo, out es para la carpeta de salida.",
    "import seleniumbase": "es el todo en uno de selenium; seleniumbase.Driver(uc=True) devuelve el driver con chromne instanciado utilizando el undetected chomedriver; proxy=proxy  para establecerles proxy al driver de forma 'protocol://ip:port'.",
    "import timeit": "para benchmarking de programas; timeit.timeit(funci\u00f3n,number,globals) : inicia el testeo y devuelve la cantidad de segundos que demora, en number la cant de veces a correrlo y globals=globals() para que lea el script completo.  ",
    "import tkinter": "para trabajar con guis sencillas.Mas en el modulo del."
  }
]